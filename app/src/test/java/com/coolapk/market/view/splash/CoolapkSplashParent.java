package com.coolapk.market.view.splash;

import android.app.Activity;
import android.os.Bundle;

/**
 * Coolapk-package parent that declares onCreate, mirroring the obfuscated
 * parent class (e.g. "Ϳ") which owns SplashActivity.onCreate(Bundle) in
 * Coolapk 16.5.1.
 */
public abstract class CoolapkSplashParent extends Activity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
    }
}
