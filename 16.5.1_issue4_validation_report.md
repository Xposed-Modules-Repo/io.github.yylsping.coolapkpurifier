# Issue #4 实机回归验证报告 — 酷安 16.5.1 (2607271)

> 依据 `16.5.1_issue4_cold-start_validation.md` 流程执行。测试日期：2026-08-25。
> 核心原则执行情况：以当前本地最新版源码构建产物为唯一被测对象，远程仓库未参与；未回退模块版本；未预设结论，实机结果说话。

---

## 一、环境

| 项目 | 值 |
| --- | --- |
| Coolapk | 16.5.1（versionCode 2607271），降级自 16.6.1，`pm install -r -t -d` 成功，应用数据未清除（登录态保留） |
| Module | coolapk-purifier 2.2.0（versionCode 10），本地最新源码构建，APK SHA-256 `f2a29ba25867c5052ac38e0a44919059cdfc96d377723825b7594ef2c80cffd9`，与设备安装产物逐字节一致 |
| 本地源码版本 | commit `6716b78`（2026-08-23）+ 本地未提交增量（26 文件，+2560/−611），构建时间 2026-08-24 23:25，晚于最后一次源码修改（23:24） |
| Android | 16（API 36） |
| Device | OPPO PLQ110（局域网 ADB 连接，地址略） |
| Hook framework | LSPosed v2.1.1 (7790)，Zygisk 模式，作用域内仅 coolapkpurifier 一个模块注入 `com.coolapk.market`（日志核实，无其他模块干扰启动流程） |
| 是否第一次安装最新版模块 | 否（2026-08-24 23:26 已安装同构建） |
| 是否清除模块解析缓存 | 否（16.5.1 条目由身份变化触发 cacheMiss 后新建；16.6.1 旧条目保留 LRU） |
| 是否清除酷安数据 | 否 |

补充：降级前先在 16.6.1 (2608212) 上按同一思路做了 20 次冷启动预验证，结果同为 **20/20 无广告**（`analysis/issue4-16.6.1/SUMMARY-16.6.1.txt`），为 16.5.1 正式验证提供了方法校准（截图时机、日志切片、LSPosed 日志轮转处理）。

## 二、阶段 A：首次适配观察（降级后第一次启动）

| 观察项 | 结果 |
| --- | --- |
| 首次适配 Toast | 出现（模块日志 `adaptationToast stage=start shown=true kind=SELECTION`，完成后 `stage=completion shown=true`） |
| 是否出现开屏广告 | 否（t1000/t2000 加载页，t3500 已是主界面信息流；视觉核实无广告素材） |
| 是否正常进入 MainActivity | 是（topResumedActivity = MainActivity，无闪退/黑屏/循环启动） |
| 开屏 Hook 解析安装 | 是。bootstrap：`cacheMiss → SPLASH_CRITICAL → splashResolveEnd candidates=1 → splashHookInstalled splash_base SplashAdActivity.onCreate source=fingerprint_strong installed=true`（727ms，DexKit 指纹，非历史类名兜底） |
| 决策 Hook | `feature.splashDecision` 解析并安装（2.23s，`rc5.Ԯ(Context,เ,String)`，候选 1/1）。本轮无 `blocked splash decision` 记录 —— 决策 Hook 装好前宿主已完成当次开屏决策，属首次适配设计语义内时序，且本轮实际无广告 |
| 其余目标 | topicRecommend / sameTopicFeed / detailSponsor / feed×2 / getter×4 全部解析成功；feed 走 fingerprint_weak + fallback_compat 双确认；getter 全部 fingerprint_strong |
| 缓存 | `cacheSaved entries=11 identity=ae559f77e199a702 coverageSettled=true`；READY @2438ms，`missingRequired=[]`，无任何 install failed / ClassNotFound / verification failed |

**阶段 A 判定：通过。** 首次适配零广告（优于设计允许的"可漏一次"），无崩溃，解析与缓存建立完整，进入正式验证。

## 三、20 次正式冷启动结果（酷安 16.5.1）

每轮流程：完全退出 → `force-stop` 并确认进程死亡（每轮 PID 均不同，真实冷启动）→ 不清模块缓存、不清应用数据 → 启动 → 观察至主界面稳定 → 采集 logcat Activity 转移 / LSPosed 模块日志切片 / t1300+t2400+t4000 截图。

| 轮次 | 冷启动 | 出现广告 | 仅闪现 | 正常进主页 | 决策 Hook 命中(blocked) | Splash Activity 拦截 | 异常日志 |
|---|---|---|---|---|---|---|---|
| 1–20 | 是（全部） | 否（0/20） | 否（0/20） | 是（20/20，topResumed 均为 MainActivity） | 是（20/20，每轮恰 1 次 `blocked splash decision via rc5.Ԯ`） | 未触发（0/20，无任何 SplashAd/FullScreenAd Activity 启动记录） | 无（0 崩溃 / 0 ANR / 0 黑屏 / 0 DEGRADED） |

汇总：

- 总次数：20
- 无广告：20（视觉：20/20 t1300 为加载页；抽查 t2400 已是主界面信息流）
- 有广告：0
- 闪现：0
- 崩溃：0
- 黑屏/卡启动：0

日志关键字归类（20 轮合计）：

- `feature.splashDecision`：安装 20/20（source=cache，`rc5.Ԯ`）
- `blocked splash decision`：20/20（决策层在宿主询问开屏时全部返回阻断）
- `SplashActivity` / `SplashAdActivity` / `FullScreenAdActivity` 启动事件：0/20（logcat ActivityTaskManager 全程仅 MainActivity，Displayed ≈ +1s）
- `specific splash hook installed`（SplashAdActivity.onCreate，source=cache）：20/20
- `finished splash`（Activity 兜层实际开火）：0/20 —— 决策层在更上游即阻断，兜底层未被动用
- `cache`（`resolver path=cache hit=true`）：20/20；`READY`：20/20；`DEGRADED`：0
- `hook install failed` / `ClassNotFound` / `verification failed` / `NoSuchMethod`：0
- Instrumentation 开屏兜底：按 2.0.1 语义被动常驻（`instrumentation bootstrap callbacks retired hooksRetained=true`），本轮无需介入

## 四、Hook 结果

- splash decision 目标是否解析：是（首次适配期 DexKit `fingerprint_strong` 解析出 `rc5.Ԯ(Context,เ,String)Z`，与 16.6.1 的 `kc5.Ԯ` 为不同混淆名，指纹跨版本生效）
- primary hook 是否安装：是（首次适配直接安装；后续 20 轮全部 cache 命中安装）
- 是否观察到 `blocked splash decision`：是（20/20）
- SplashActivity fallback：被动模式，在位未触发
- SplashAdActivity fallback：specific hook 安装 20/20，未触发（决策层先拦）
- FullScreenAdActivity fallback：被动模式，在位未触发
- Instrumentation fallback：被动常驻（2.0.1 行为），未触发

## 五、最终结论

### 结论 A

> 20/20 正常（另 16.6.1 预验证亦 20/20 正常）。当前最新版在酷安 16.5.1 上已无法复现 Issue #4。认为新版很可能已经通过现有新增 Hook（`feature.splashDecision` 决策链覆盖 + SplashAdActivity specific hook + 被动 Activity 兜底）顺手修复该问题，不继续针对 Issue #4 做源码或架构级修改。

按 md 第七节约束：未做任何 HookCoordinator 重构、状态机改造、Instrumentation 扩面或模糊匹配扩大；本次验证**未修改任何源码**。

依据第九节证据门槛逐条对照：20 次正式冷启动复现真实广告次数为 0；无日志证明任何已知 Hook 未安装；无未知广告 Activity/决策入口出现；无稳定复现步骤；无失效点。**不满足任何允许修改源码的条件。**

## 六、遗留说明

- 测试后设备保持酷安 16.5.1（数据/登录完好），模块解析缓存现含 16.5.1 与 16.6.1 两个版本条目；是否升回 16.6.1 由人工决定。
- 原始证据：`analysis/issue4-16.5.1/`（首次适配 + 20 轮截图/日志切片）、`analysis/issue4-16.6.1/`（16.6.1 预验证）。测试期间 LSPosed 模块日志在 19:57:42 发生一次文件轮转（16.6.1 第 13 轮期间），轮转前后切片已合并归档，不影响证据完整性。
- 唯一观察到的非理想点：首次适配当轮决策 Hook 安装（2.23s）晚于宿主开屏决策时刻，该轮无 `blocked splash decision` 记录。这是已知设计语义（首次适配允许漏一次），且该轮实际未出广告，不构成 Issue #4 证据，不据此修改。
